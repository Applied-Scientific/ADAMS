Use only `qa/agentic_testkit_v1/configs_txt/brd4_holo_3mxf.txt` as run contract (key=value). Do NOT read scenarios.yaml for this run. Parse config as plain text key=value (no yaml/ruamel imports). Run docking validation only (no MD): standardize ligands from `ligand_input`; run ligand preprocessing with `microstate_enumeration`; convert ligands to pdbqt; clean receptor with `chain_to_keep`, `residue_range_start`, `residue_range_end`, `keep_heterogens`, `keep_water`; protonate receptor at `pH`; then run docking with `backend`, `mode`, `num_pockets`, `num_poses`, `production_gridsize`, `docking_centers`, and `out_folder`, using `input_data` from the generated docking-ready ligand mapping CSV and `receptor` from the protonated receptor returned by preprocessing. Treat `xray_map`, `xray_map_txt`, and any `redocking_report.*` keys as config-only metadata, not `run_docking` arguments. Use returned artifact paths between stages. Required outputs in `<out_folder>/docking/production/summaries/`: `production_docking_results.csv`, `failed_combinations.csv`, `production_pose_manifest.csv`, `top10_per_ligand_explicit.csv`. Use built-in local file tools; do not ask the user to paste local files. Do not use run_python_code unless absolutely required by a pipeline tool call. If any required key/path is missing or ambiguous, stop immediately with one consolidated error and do not attempt fallback parsing/discovery via run_python_code.
