Use only `qa/agentic_testkit_v1/configs_txt/brd4_holo_3mxf.txt` as key=value contract. Do NOT read scenarios.yaml. This is a SHORT MD smoke run: use contract values as authoritative (`md.tops`, `md.production_nsteps=50000`, `md.production_dt_fs=2.0`, `md.soluble_eq_nsteps_scale=0.1`). Do not run custom python code. Do not import `__future__`, `subprocess`, `discover_paths`, `yaml`, or `ruamel`. Resolve paths relative to the contract directory first, then CWD. If the unattended contract override provides `docking_root`, `docking_csv`, `protein_file`, `ligand_input`, `md_workdir`, `gromacs_path`, `ambertools_path`, and `gromacs_binary_type`, use those exact values and do not rediscover or glob alternatives. Otherwise resolve docking root strictly from `out_folder` (or `agent_data/outputs/<out_folder>`); do not scan unrelated folders. Require `<docking_root>/docking/production/summaries/production_docking_results.csv` and newest `<docking_root>/preprocessing/receptors/*_protonated.pdb`; fail clearly if missing. Run only: `run_md_prepare` (soluble) -> `run_md_simulation` -> `run_md_analysis` with contract md.* parameters. When calling `run_md_simulation`, pass unprefixed runtime overrides `production_nsteps=50000`, `production_dt_fs=2.0`, and `soluble_eq_nsteps_scale=0.1` explicitly; do not rely on defaults from `md.mdp`. Report exact paths for `lig_prepare_summary.json`, `lig_prepare_failures.csv`, and `md_runtime_summary.csv`. Use built-in local file tools; do not ask the user to paste local files. Do not use run_python_code unless absolutely required by a pipeline tool call. If any required key/path is missing or ambiguous, stop immediately with one consolidated error and do not attempt fallback parsing/discovery via run_python_code.
